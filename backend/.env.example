# Google Cloud Vision — required for retrieval-augmented listing generation
# Point to your service account key file (absolute path)
GOOGLE_APPLICATION_CREDENTIALS=/absolute/path/to/key.json

# Anthropic — required for Claude-powered listing generation
ANTHROPIC_API_KEY=
