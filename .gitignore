node_modules/
dist/
__pycache__/
*.pyc
*.db
.venv/
venv/
backend/uploads/*
!backend/uploads/.gitkeep
.env
.env.local
.env.test
.env.*.local
# Any other .env variant (e.g. .env.staging) — keep secrets out of git by default.
.env.*
# ...but keep example templates tracked so the expected shape is documented.
!.env.example
!.env.*.example
.vite/
backend/.secrets/
*.serviceaccount.json

# macOS noise
.DS_Store

# Design handoff (local-only — do not commit)
Cosello.zip
DESIGN.md
styleguide.html
design_handoff_cosello/

# Brainstorming companion artifacts (local-only)
.superpowers/

# Brainstorm server runtime state — ephemeral process artifacts, not durable docs.
# The content/*.html files stay tracked; the per-session state/ dir does not.
docs/superpowers/brainstorm/*/state/

# Large school bootstrap dataset — loaded once into the school_seed table, not
# committed (hundreds of MB). The 20-row backend/data/us_higher_ed.csv sample stays tracked.
backend/data/Most-Recent-Cohorts-Institution.csv

# Supabase CLI temp/runtime artifacts.
supabase/.temp/

# Stray screenshots dropped at the repo root (durable design refs live in docs/).
Screenshot*.png
