node_modules/
dist/
__pycache__/
*.pyc
*.db
.venv/
venv/
backend/uploads/*
!backend/uploads/.gitkeep
.env
.env.local
.vite/
backend/.secrets/
*.serviceaccount.json

# macOS noise
.DS_Store

# Design handoff (local-only — do not commit)
Cosello.zip
DESIGN.md
styleguide.html
design_handoff_cosello/

# Brainstorming companion artifacts (local-only)
.superpowers/

# Brainstorm server runtime state — ephemeral process artifacts, not durable docs.
# The content/*.html files stay tracked; the per-session state/ dir does not.
docs/superpowers/brainstorm/*/state/
